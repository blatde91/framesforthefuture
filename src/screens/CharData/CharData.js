import React, {Component} from 'react';
import {
  View,
  Text,
  TouchableOpacity,
  TextInput,
  Alert,
  ActivityIndicator,
  Button,
} from 'react-native';

class CharData extends Component {
  render() {
    return (
      <View style={{flex: 1}}>
        <View style={{flex: 1}}>
          <Text>{}</Text>
        </View>
      </View>
    );
  }
}

export default CharData;
